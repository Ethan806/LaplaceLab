prime ? 4;
s ? 1;
j ? 0;

loop(prime) {
    q ? prime / s;
    m ? 1;
    l ? 0;

    loop(q) {
        if(m ?? q) {
            l ? 1;
        };
        m ? m + 1;
    };

    if(l ?? 1) {
        j ? j + 1;
    };
  s?s+1;
};

if(j ?? 2) {
    display("it is a prime number");
};
else {
    display("It is not a prime number");
};
